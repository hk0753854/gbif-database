bundle:
  name: animal-database

include:
  - resources/*.yml

targets:
  dev:
    mode: development

  prod:
    mode: production
    workspace:
      root_path: /Workspace/Users/newstatus.vinhoverde@gmail.com/.bundle/${bundle.name}/${bundle.target}